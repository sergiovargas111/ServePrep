import PagePlaceholder from "@/components/page-placeholder";

export default function DashboardPage() {
  return (
    <PagePlaceholder
      title="Dashboard"
      description="Aggregated progress, streaks, weak-spot callouts, and certificate status across every tab will live here."
    />
  );
}
